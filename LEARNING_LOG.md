# 翱翔之翼官网平台学习记录

这个文档会记录我们每次对网站做了什么、为什么这样做，以及你作为网页开发初学者需要理解的知识点。后续每次调整功能，我都会同步更新这里。

## 2026-05-31：确定项目方向并创建首版工程

### 本次目标

把“阜阳师范大学中科协‘翱翔之翼’大学生志愿服务队官网”从普通展示页升级为一个可以长期更新的平台，包含官网展示、志愿者管理、活动报名、证书生成、课程学习、器材教具、学生成果和竞赛成果等模块。

### 我做了什么

1. 查看当前项目目录，确认它是空目录，还不是 Git 仓库。
2. 用 Vite 创建了一个 React 项目。
3. 安装了项目依赖。
4. 从你提供的真实活动照片中复制了一部分到 `public/media`，作为网站首版视觉素材。
5. 修改了网页标题、语言和 SEO 描述，让网站未来更适合被搜索引擎识别。
6. 创建了这份 `LEARNING_LOG.md` 学习记录文档。

### 这些操作是什么意思

`Vite` 是一个前端项目构建工具。你可以把它理解为“网页开发启动器”：它帮我们快速创建项目、启动本地预览服务器、打包上线文件。

`React` 是一个前端框架。它适合做这种有很多栏目、很多交互、后续还要持续增加功能的网站平台。

`public/media` 是静态资源目录。放在这里的图片可以直接被网页访问，例如 `/media/sxx-01.jpeg`。

`index.html` 是网站最基础的入口文件。里面的 `<title>` 会显示在浏览器标签页，也会影响搜索结果标题；`description` 会帮助搜索引擎理解网站内容。

### 为什么这样做

如果只写一个普通 HTML 页面，后续你要加“报名系统”“证书生成”“志愿者管理”会很难维护。React 项目更适合把不同功能拆成模块，后面可以逐步接数据库、后台接口和登录权限。

### 你需要先理解的概念

- 前端：用户在浏览器里看到和操作的页面。
- 后端：负责保存数据、处理报名、登录、证书生成等逻辑的服务器程序。
- 数据库：保存志愿者、活动、报名、课程、器材等信息的地方。
- 静态资源：图片、视频、图标、PDF 等文件。
- 本地开发服务器：只在你电脑上运行的预览环境，用来开发和调试。

### 当前项目结构

```text
翱翔之翼/
  index.html
  package.json
  src/
    App.jsx
    App.css
    index.css
    main.jsx
  public/
    media/
      sxx-01.jpeg ...
  LEARNING_LOG.md
```

### 下一步

我会把默认的 Vite 示例页面替换成“翱翔之翼”平台首版页面，包含栏目导航、数据概览、活动报名、志愿者管理概览、课程与器材模块，以及证书生成演示按钮。

## 2026-05-31：完成平台首版前端原型

### 本次目标

把默认 React 示例页改成真正可看的“翱翔之翼”平台首页，并把你提出的功能栏目先全部落到页面上。

### 我做了什么

1. 重写了 `src/App.jsx`，这是 React 页面主体。
2. 重写了 `src/App.css`，这是页面样式。
3. 简化了 `src/index.css`，保留全局字体、背景、滚动等基础样式。
4. 做了这些栏目：
   - 翱翔之翼信息介绍栏
   - 志愿者信息管理栏
   - 活动展示栏
   - 志愿者招新栏
   - 小学生成果展示栏
   - 大学生学科竞赛栏
   - 活动报名栏
   - 活动报名后的志愿者证书生成按钮
   - 少儿编程学习栏
   - 器材教具栏
5. 活动报名现在可以填写姓名、学院/专业、联系方式、活动和岗位。
6. 报名数据会暂存在浏览器的 `localStorage`。
7. 报名后，志愿者管理表格会出现记录，并可以点击“生成证书”。

### 这些代码是什么意思

`App.jsx` 是 React 组件文件。你可以把一个组件理解为“页面上的一块功能区域”。现在为了让你更容易学习，我先把所有模块放在一个文件里，后续功能多了再拆成多个组件文件。

`useState` 是 React 用来保存页面状态的工具。比如报名表里输入的姓名、电话，以及报名记录列表，都属于状态。

`localStorage` 是浏览器自带的小型本地存储。它适合做学习阶段的演示：刷新页面后，报名记录仍然能保留。但它不是正式数据库，正式上线后要换成后端数据库。

`map` 是 JavaScript 里常见的数组方法。页面里的导航、活动卡片、课程列表、器材列表，都是通过数组 `map` 自动生成的。这样以后加一个活动，只需要往数组里加一条数据，不需要复制一大段 HTML。

### 为什么这样做

你希望网站以后可以不停更新新功能，所以首版不能只写死一张页面。我把内容先做成“数据数组 + 页面循环渲染”的形式，这是平台开发的基础思路：数据变了，页面自动跟着变。

报名和证书先做前端演示，是为了让你尽快看到完整流程：

```text
填写报名表 -> 进入志愿者管理表 -> 点击生成证书
```

后续我们再把这条流程升级成：

```text
填写报名表 -> 后端保存到数据库 -> 管理员审核 -> 签到签退 -> 记录时长 -> 生成可核验证书
```

### 你需要理解的新概念

- 组件：页面中可复用的一块功能。
- 状态：页面会变化的数据，例如报名表内容。
- 表单：用户输入信息的地方。
- 本地存储：浏览器临时保存数据的地方。
- 数据驱动页面：先整理数据，再用代码把数据渲染成页面。

### 当前限制

1. 现在还没有真正的登录系统。
2. 现在还没有后端和数据库。
3. 证书只是网页弹窗演示，还不能导出 PDF。
4. 管理员审核、服务时长、证书编号核验还没有真实业务规则。

这些限制不是问题，首版的任务是先把功能流程跑通。后面每次加功能，我们会逐步把“演示版”替换成“正式版”。
## 2026-05-31：给演示数据增加清空按钮

### 我做了什么

1. 在志愿者信息管理栏加入“清空演示数据”按钮。
2. 新增 `clearRegistrations` 方法，用来清除页面中的报名记录、本地浏览器缓存和正在打开的证书弹窗。

### 这一步是什么意思

目前报名记录还没有连接真正数据库，而是暂时存到浏览器的 `localStorage` 里。你可以把 `localStorage` 理解成浏览器给这个网站准备的小记事本，刷新页面后数据还在。

测试报名功能时会产生假数据，所以需要一个清空按钮，方便学习、演示和调试时随时重置。

### 为什么这样做

这让首版原型更适合教学和演示。等以后接入后台数据库时，这个按钮可以升级成管理员后台里的“批量管理报名记录”功能。
## 2026-05-31：优化首屏冲击感、官方标识与第二屏内容

### 本次目标

根据你的反馈，把首页从普通展示页改成更有冲击力的项目官网首屏，并把“活动成果展示、新闻报道”提前到第二屏。

### 我做了什么

1. 下载并放入中国科学技术协会官方标识图片，保存到 `public/media/cast-logo.png`。
2. 把网站名称更新为：阜阳师范大学中科协“翱翔之翼”大学生志愿者服务项目。
3. 重做顶部品牌栏：左侧使用中科协标识，右侧显示项目全名，导航改成深色科技风按钮。
4. 把第一页改成全屏大图视觉，用真实活动照片做背景，让访客一进来先看到项目现场感。
5. 新增“活动成果展示与新闻报道”第二屏，放在首屏后面、数据概览前面。
6. 给页面增加科技感背景：网格线、深色底、光线层次和更强的按钮视觉。

### 这一步是什么意思

`header` 是网页顶部区域，通常放 logo、项目名称和导航。它决定网站的第一印象，也影响用户能不能快速知道“这是谁的网站”。

`hero section` 是首页第一屏。官网第一屏不只是好看，它要回答三个问题：这是什么项目、它做什么、为什么可信。我们用真实活动照片做背景，是为了让项目从一开始就有现场感和可信度。

`第二屏` 是用户向下滚动后马上看到的内容。你希望活动成果和新闻报道更重要，所以我把它放在首屏后面，让访客不用找就能看到项目成果。

### 为什么这样做

这个网站未来要靠搜索进入，陌生人打开后通常只会先看几秒钟。首屏负责吸引和建立信任，第二屏负责证明项目真的做过事情、有成果、有报道。后面的报名、管理、课程和器材模块再承担具体功能。
## 2026-05-31：补充修正页面滚动与科技背景

### 我做了什么

1. 把页面外层的 `overflow: hidden` 改成了 `overflow-x: hidden`。
2. 把科技背景里的圆形光晕改成更简洁的线性光感和网格线。

### 这是什么意思

`overflow` 控制页面内容超出容器后怎么显示。`overflow: hidden` 会把超出部分藏起来，如果放在最外层，页面可能就不能继续往下滚动。现在改成 `overflow-x: hidden`，意思是只隐藏横向多出来的部分，纵向内容仍然可以滚动。

科技感不只是“加一个蓝色光圈”。更适合官网的做法，是用稳定的深色底、网格线、光线层次和真实活动照片来建立专业感。
## 2026-05-31：把每一页统一成科技感视觉冲击风格

本次调整内容：
- 修改 `src/App.css`，把原来后半部分的白底栏目改成深色科技风分屏。
- 给每个主要栏目设置不同的真实活动照片作为背景，例如团队介绍、志愿者管理、活动展示、报名、少儿编程、器材教具等栏目。
- 给所有栏目统一加入网格线、扫描线、玻璃质感卡片、发光边框，让后续新增功能也能自动沿用这套视觉风格。
- 保留原有功能逻辑，没有改报名表、志愿者记录、证书生成这些 React 代码。

我为什么主要改 CSS：
- React 的 `App.jsx` 负责“页面有什么内容和功能”，CSS 负责“这些内容长什么样”。
- 这次你的需求是视觉风格统一，所以优先改 CSS，可以一次影响多个栏目，避免每个栏目重复写一遍样式。
- 后续如果新增“后台管理”“证书查询”“课程资源上传”等功能，只要继续使用已有的 `content-section`、`split-section`、`signup-section` 这些 class，就能直接获得同样的科技感页面效果。

你可以这样理解：
- `#overview`、`#volunteers`、`#activities` 这类以 `#` 开头的是页面栏目 ID，用来控制某一个具体栏目。
- `.content-section`、`.split-section`、`.signup-section` 这类以 `.` 开头的是通用样式类，用来控制一组页面的共同外观。
- `background-image` 同时叠了三层：深色遮罩、科技网格、真实活动照片。这样照片不会抢文字，页面也更有冲击力。
## 2026-05-31：把“志愿者招新”改成最后一页“加入我们”

本次调整内容：
- 修改 `src/App.jsx` 的导航顺序，把原来的“志愿者招新”移动到最后，并改名为“加入我们”。
- 把页面里的对应栏目也移动到所有内容之后，保证菜单顺序和实际页面顺序一致。
- 把“加入我们”内容改成组织部门介绍，包括科普教学部、课程研发部、活动运营部、器材教具部、宣传影像部、竞赛与项目部。
- 修改 `src/App.css`，新增 `department-grid` 部门卡片样式，让部门介绍保持科技感视觉。
- 给各页面栏目加入 `scroll-margin-top`，避免点击导航打开栏目时被顶部菜单遮住标题。

这一步学到什么：
- `navItems` 是导航菜单的数据源，调整数组顺序，就能调整顶部菜单显示顺序。
- 页面里的 `<section id="recruit">` 是真正的栏目位置；如果只改导航不移动 section，菜单虽然在最后，但页面内容还会出现在中间，所以这次两个地方都要改。
- `href="#recruit"` 这种写法叫锚点跳转，点击后浏览器会打开同一个页面里的对应栏目。后续如果要做真正的多页面路由，我们再学习 React Router。
## 2026-06-01：把个人账户入口移动到首页右上角

### 我做了什么

1. 从主导航中移除了“个人账户”，避免导航栏越来越长、重点不清楚。
2. 在顶部栏右上角新增了一个“登录/注册”小按钮，点击后跳转到个人账户登录注册页面。
3. 修正了个人账户页、平台建设页的背景图片路径，并补齐登录表单、登录方式按钮、平台路线图等样式。

### 这一步学什么

- `href="#account"` 是页面内锚点跳转，浏览器会滚动到 `id="account"` 的区域。
- 导航栏适合放网站栏目，账户入口通常独立放在右上角，这样更符合大多数网站的使用习惯。
- 目前手机号、微信、QQ 登录还是前端学习原型；真正上线时，需要后端账号系统、短信验证码服务，以及微信/QQ 的 OAuth 授权接入。
- 窄屏页面也要单独适配：我把顶部栏在手机宽度下改成“标题在左、登录按钮在右、导航在下一行”，这样按钮仍然符合右上角入口的习惯。

## 2026-06-01：准备上传 GitHub 与版本记录

### 我做了什么

1. 新建 `CHANGELOG.md`，把当前官网原型记录为 `v0.1.0`。
2. 把 `package.json` 和 `package-lock.json` 的版本号从 `0.0.0` 调整到 `0.1.0`，让代码版本和版本记录一致。
3. 检查了本机环境：Git 已安装，但 GitHub CLI `gh` 没有安装；当前文件夹还没有初始化为 Git 仓库。

### 这一步学什么

- `CHANGELOG.md` 是项目版本记录，适合写“这个版本新增了什么、改了什么、怎么验证”。
- `package.json` 里的 `version` 是前端项目自己的版本号，通常和发布记录同步更新。
- 上传到 GitHub 一般需要三件事：本地 Git 仓库、一次提交记录、一个 GitHub 远程仓库地址。

## 2026-06-01：确认 GitHub 仓库地址

### 我做了什么

1. 使用你提供的仓库地址：`https://github.com/lwm77/-Soaring-Wings-College-Students-Science-Technology-Volunteer-Service-Program`。
2. 检查远程仓库可以访问，并且目前没有返回已有分支，判断它很可能是一个空仓库。
3. 查看 `.gitignore`，确认 `node_modules` 和 `dist` 已被排除，上传时不会把依赖文件和打包产物提交进去。

### 这一步学什么

- 空仓库第一次上传时，通常要在本地 `git init`，再添加远程地址 `git remote add origin ...`。
- `.gitignore` 用来告诉 Git 哪些文件不要进入版本管理，比如依赖目录、构建产物和本地缓存。
- `git ls-remote` 可以查看远程仓库已有的分支和提交；没有输出但命令成功，通常表示仓库可访问但还没有提交。

## 2026-06-01：完成第一次 GitHub 上传

### 我做了什么

1. 在项目文件夹中初始化 Git 仓库，并使用 `main` 作为主分支。
2. 添加远程仓库 `origin`，指向你提供的 GitHub 地址。
3. 暂存官网源码、图片素材、学习记录和版本记录，提交为 `v0.1.0 initial volunteer platform`。
4. 将 `main` 分支推送到 GitHub，完成第一次线上代码备份。

### 这一步学什么

- `git add` 是把文件放进“准备提交区”，表示这些文件要进入下一次版本记录。
- `git commit` 是生成一次本地版本快照，以后可以通过提交号追溯。
- `git push` 是把本地提交上传到 GitHub，让云端仓库拥有同样的代码历史。

## 2026-06-02：参考中国科协官网调整首页信息架构

### 我做了什么

1. 参考中国科协官网首页“新闻动态优先、服务入口随后展开”的结构，把本网站前半段调整为：成果与新闻、项目介绍、红色学习、志愿服务动态。
2. 在首页第一屏新增新闻动态面板，展示项目动态、成果展示、合作交流等重点信息。
3. 把原来容易显得分散的功能清单合并成五个入口：内容宣传、成果资料、报名服务、后台管理、课程资源。
4. 增加“功能详情页”，点击平台建设中的分类开关后，可以打开对应类别的详情内容。
5. 收紧各栏目高度，让页面不再显得太空，同时保留科技感背景、真实照片、网格线和玻璃质感。

### 这一步学什么

- 网页开发不只是写代码，也包括“信息架构”。信息架构就是决定哪些内容先出现、哪些内容合并、哪些内容放到后面。
- `useState` 可以保存当前选中的功能分类。点击不同按钮时，React 会根据状态重新显示对应内容。
- `href="#platformDetail"` 是锚点跳转，点击后会跳到同一个网页里的详情区域。以后如果接入 React Router，就可以把它升级成真正的独立页面地址。
- CSS 里的 `grid-template-columns` 可以把首页做成左右结构：左边放大标题，右边放新闻面板。手机端再改成单列，保证小屏幕也能看。

### 为什么这样做

你的网站不是单纯课程网站，而是一个志愿服务项目官网。陌生人搜索进入时，应该先看到“这个组织是谁、最近做了什么、有哪些成果、是否可信”。少儿编程课程、器材教具、后台管理这些功能很重要，但更适合放在后半段或详情页里，让真正需要使用的人再进入。

## 2026-06-02：接入后端服务和 SQLite 数据库

### 本次目标

把“活动报名”从前端本地演示，升级为可以写入数据库的真实后台流程。现在网站已经开始有“前端页面 + 后端接口 + 数据库”的三层结构。

### 我做了什么

1. 安装了 `express` 和 `cors`。
2. 新建 `server/index.js`，作为后端服务入口。
3. 新建 `server/database.js`，负责创建 SQLite 数据库和读写报名数据。
4. 新建 `src/api.js`，让前端通过接口请求后端。
5. 修改 `src/App.jsx`，让活动报名优先保存到数据库。
6. 如果后端没有启动，前端会自动切回本地演示模式，不影响你继续学习和预览。
7. 把版本号升级到 `0.2.0`，并更新 `CHANGELOG.md`。

### 这些概念是什么意思

前端：你在浏览器里看到的页面，比如首页、报名表、志愿者管理栏。

后端：运行在电脑或服务器上的程序，负责接收前端请求、处理数据、保存数据。这里的入口是 `server/index.js`。

接口：前端和后端约定好的“通信地址”。比如前端提交报名表时，会请求 `POST /api/registrations`。

数据库：长期保存数据的地方。现在我们用 SQLite，它会在项目里的 `data/aoxiang.sqlite` 生成一个数据库文件。

数据表：数据库里的分类存储区域。现在有 `registrations` 报名表、`activities` 活动表、`accounts` 账号表。

### 为什么先用 SQLite

SQLite 不需要额外安装大型数据库软件，Node 现在可以直接使用它。对学习来说，它能让你先理解“数据怎么保存、接口怎么调用、前后端怎么连接”，不用一开始就被数据库服务器配置卡住。

以后正式部署到学院服务器或云服务器时，可以继续用 SQLite 做小规模运行；如果报名、文章、文件、账号越来越多，再迁移到 MySQL 或 PostgreSQL。

### 你现在怎么运行

打开两个终端：

```bash
npm run server
```

这个命令启动后端服务，默认地址是 `http://127.0.0.1:3001`。

```bash
npm run dev
```

这个命令启动前端页面，默认地址是 `http://127.0.0.1:5173`。

### 报名数据现在怎么流动

```text
填写活动报名表
  -> 前端调用 src/api.js
  -> 请求后端 POST /api/registrations
  -> server/index.js 接收请求
  -> server/database.js 写入 SQLite
  -> 前端刷新志愿者管理表格
```

### 当前仍然只是第一版后端

现在已经有数据库，但还没有正式管理员后台、真实登录鉴权、短信验证码、微信/QQ OAuth、文件上传审核和证书 PDF 导出。下一阶段可以继续做：账号注册登录、后台文章发布、活动管理、资料上传、证书导出、权限管理。
## 2026-06-02：如何验证后端是否真的接上

这次验证分成两步：

1. 先验证后端接口：访问 `http://127.0.0.1:3001/api/health`。如果返回 `ok: true` 和 `database.type: SQLite`，说明后端服务和数据库文件已经正常工作。
2. 再验证前端页面：访问 `http://127.0.0.1:5173/#volunteers`。页面里出现“SQLite 数据库已连接”，说明网页已经能和后端通信。

为什么要分两步：前端页面打不开，不一定是数据库错；数据库接口打不开，也不一定是网页代码错。拆开验证，就能很快知道问题在哪一边。

本次我实际验证到：

- 后端接口正常：`BackendOk: true`
- 数据库类型：`SQLite`
- 前端页面正常：`http://127.0.0.1:5173`
- 志愿者管理页面能显示数据库连接状态

你自己验证时，需要打开两个终端：

```powershell
npm run server
```

这个命令启动后端，负责数据库和接口。

```powershell
npm run dev
```

这个命令启动前端，负责浏览器看到的网站页面。

如果命令执行不了，先确认终端所在目录是项目根目录：

```powershell
cd D:\学校学习\ai学习\Codex\网页开发\翱翔之翼
```

然后再执行上面的两个命令。两个服务都要开着，网页才能连接数据库。
## 2026-06-02：升级为可切换 PostgreSQL / SQLite 数据库

这次我们把后端数据库从“只能使用 SQLite”升级为“默认 SQLite，也可以切换 PostgreSQL”。

我做了什么：

1. 安装 `pg`：这是 Node.js 连接 PostgreSQL 的工具包。
2. 安装 `dotenv`：它可以读取 `.env` 文件里的数据库配置。
3. 重写 `server/database.js`：让它根据环境变量判断使用 SQLite 还是 PostgreSQL。
4. 重写 `server/index.js`：把接口改成异步写法，因为 PostgreSQL 是网络数据库，请求数据库需要等待返回结果。
5. 新增 `.env.example`：告诉你正式部署时应该怎么填写 PostgreSQL 连接信息。
6. 更新 `README.md` 和 `CHANGELOG.md`：记录这次升级和验证方法。

为什么不直接删掉 SQLite：

SQLite 很适合本地学习，因为不用安装复杂数据库。PostgreSQL 更适合正式部署，因为它支持多人访问、权限管理、长期运行和更复杂的数据结构。保留两种模式，可以让你本地继续学习，正式上线时再切换 PostgreSQL。

你可以这样理解：

```text
本地学习：不配置 DATABASE_URL -> 自动使用 SQLite
正式部署：配置 DATABASE_URL -> 自动使用 PostgreSQL
```

`.env` 是什么：

`.env` 是“环境变量配置文件”，里面放数据库地址、端口、密码等不适合写进代码的内容。它不会上传到 GitHub，避免泄露密码。

后端现在怎么判断数据库：

```text
如果存在 DATABASE_URL，并且 DATABASE_CLIENT 不是 sqlite
  -> 使用 PostgreSQL
否则
  -> 使用 SQLite
```

这一步很重要，因为后续我们要做管理员后台、文章发布、活动管理、资料上传、账号权限时，都需要稳定的数据存储。现在数据库层先打好基础，后面扩展功能会更顺。
## 2026-06-03：开始阿里云 Ubuntu 服务器部署

### 本次目标

把本地开发的网站，逐步部署到阿里云 ECS 服务器上，让它从“只能在自己电脑访问”变成“可以通过公网 IP 或域名访问”。

### 当前进展

你已经完成了非常关键的一步：通过 SSH 成功登录到了阿里云 Ubuntu 服务器。

服务器公网 IP 是：

```text
121.40.156.210
```

登录成功后，终端里出现类似下面这样的提示：

```text
root@iZq78cdqbdszzqZ:~#
```

这说明你现在已经不是在操作自己电脑，而是在操作云服务器。

### 这一步是什么意思

SSH 可以理解为“用命令行远程控制服务器”。以后网站上线、安装数据库、启动后端、配置 Nginx，基本都要在这个服务器终端里完成。

`root` 是 Linux 系统里的超级管理员账号，权限很高。学习阶段可以先用它操作，但正式长期运行时，后续更推荐创建普通用户，降低误操作风险。

### 接下来要做什么

下一步是在服务器里安装这些基础软件：

- Git：从 GitHub 下载项目代码。
- Node.js：运行网站后端。
- PostgreSQL：保存报名、活动、账号等数据。
- Nginx：把网站发布到公网。

我已经新增了 `DEPLOY_UBUNTU.md`，里面会记录 Ubuntu 服务器部署步骤，方便你以后复盘。

### 已完成：安装服务器基础环境

你在服务器上完成了基础工具安装：

```bash
apt update
apt upgrade -y
apt install -y curl git nginx postgresql postgresql-contrib
```

这一步的意义是先让服务器具备“跑网站”的基本能力：

- Git 用来从 GitHub 拉取代码。
- Nginx 用来把网站发布到公网。
- PostgreSQL 用来保存正式数据。
- curl 用来下载后续安装脚本。

### 已完成：安装 Node.js

你检查服务器时发现 `node` 和 `npm` 都不存在，这说明本地电脑有 Node.js 不代表云服务器也有。服务器是一台新的远程电脑，运行后端服务也需要单独安装 Node.js。

你执行了：

```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
apt install -y nodejs
node -v
npm -v
```

安装结果：

```text
node v22.22.2
npm 10.9.7
```

这说明服务器现在已经具备运行 Node.js 后端的能力。

### 已完成：创建 PostgreSQL 数据库

你进入了 PostgreSQL 命令行：

```bash
sudo -u postgres psql
```

然后创建了网站要用的数据库和数据库用户：

```sql
CREATE DATABASE aoxiang;
CREATE USER aoxiang_user WITH PASSWORD 'Aoxiang_2026_db!';
GRANT ALL PRIVILEGES ON DATABASE aoxiang TO aoxiang_user;
\q
```

执行结果里出现：

```text
CREATE DATABASE
CREATE ROLE
GRANT
```

这说明数据库、用户和授权都已经创建成功。

这一步的意义是：以后志愿者报名、活动信息、账号信息等正式数据，不再只存在浏览器或本地 SQLite，而是可以保存到服务器 PostgreSQL 数据库里。

### 已完成：从 GitHub 拉取项目代码

你在服务器上执行了：

```bash
cd /opt
git clone https://github.com/lwm77/-Soaring-Wings-College-Students-Science-Technology-Volunteer-Service-Program.git aoxiang
cd /opt/aoxiang
npm install
```

执行结果显示依赖安装完成：

```text
added 216 packages
found 0 vulnerabilities
```

这说明项目代码已经放到了服务器的 `/opt/aoxiang` 目录，运行所需的 Node.js 依赖也已经安装好。

`npm install` 的作用是根据项目里的 `package.json` 和 `package-lock.json` 下载依赖，例如 React、Vite、Express、PostgreSQL 连接库等。

### 已完成：配置服务器环境变量

你先从 `.env.example` 复制出了正式配置文件：

```bash
cp .env.example .env
```

第一次查看 `.env` 时，它还是示例内容，里面的数据库用户名和密码还没有换成我们刚创建的 PostgreSQL 用户。后来你把它改成了：

```env
DATABASE_CLIENT=postgresql
DATABASE_URL=postgresql://aoxiang_user:Aoxiang_2026_db!@127.0.0.1:5432/aoxiang
DATABASE_SSL=false
PORT=3001
```

这一步的意义是告诉后端：

- 使用 PostgreSQL，而不是默认 SQLite。
- 数据库地址在服务器本机 `127.0.0.1`。
- 数据库端口是 `5432`。
- 数据库名是 `aoxiang`。
- 后端服务端口是 `3001`。

你还执行了：

```bash
sudo -u postgres psql -d aoxiang -c "GRANT ALL ON SCHEMA public TO aoxiang_user;"
```

这一步是给 `aoxiang_user` 补充 public schema 权限，避免后端初始化数据表时因为权限不足而失败。

## 2026-06-03：验证 PostgreSQL 后端公网访问成功

### 这次完成了什么

你在服务器上启动了后端服务：

```bash
npm run server
```

随后用浏览器访问：

```text
http://121.40.156.210:3001/api/health
```

页面返回了：

```json
{
  "ok": true,
  "service": "aoxiang-api",
  "database": {
    "type": "PostgreSQL",
    "host": "127.0.0.1:5432"
  }
}
```

这说明后端部署和数据库连接已经成功。

### 这一步代表什么

`/api/health` 是健康检查接口。它不负责展示漂亮页面，而是专门告诉我们三件事：

- 后端服务有没有启动。
- 浏览器能不能从公网访问到服务器。
- 后端现在连接的是哪一种数据库。

这次返回里的 `"type": "PostgreSQL"` 很重要，它说明服务器上的正式数据库已经接入成功，不再只是本地演示用的 SQLite。

### 为什么之前会看到 SQLite 警告

服务器终端里出现过 `SQLite is an experimental feature`，这个提示容易让人误会。

原因是代码里保留了 SQLite 作为本地学习模式的备用方案，所以 Node.js 在读取代码时会提示 SQLite 模块是实验功能。但真正是否使用 SQLite，要看后端健康检查接口返回的数据库类型。

现在健康检查已经显示 `PostgreSQL`，所以这一步可以判定为成功。

### 下一步要做什么

接下来要把“后端临时运行”升级为“正式运行”：

- 用 PM2 管理后端，让它关闭 SSH 后也不会停。
- 构建前端页面，生成 `dist`。
- 配置 Nginx，让用户直接访问 `http://121.40.156.210` 就能打开官网。
- 让网页里的 `/api` 请求自动转发到后端 `3001` 端口。

## 2026-06-03：前端官网公网访问成功

### 这次完成了什么

你打开了：

```text
http://121.40.156.210
```

浏览器已经成功显示官网首页，页面标题和首屏视觉都能正常加载。

这说明前端页面已经不只是本地开发预览，而是通过阿里云服务器对外提供访问了。

### 这一步代表什么

前端上线主要包含三个动作：

1. `npm run build`：把 React 开发项目打包成浏览器可以直接访问的静态文件。
2. 把 `dist` 里的文件复制到 `/var/www/aoxiang`。
3. 通过 Nginx 把 `/var/www/aoxiang` 发布到服务器的 `80` 端口。

用户访问 `http://121.40.156.210` 时，浏览器会先找到阿里云服务器，然后 Nginx 会把官网页面文件返回给浏览器。

### 为什么这是一个重要节点

到这里，我们已经完成了第一轮上线闭环：

```text
浏览器访问公网 IP
-> Nginx 返回前端页面
-> 前端页面调用后端接口
-> 后端连接 PostgreSQL 数据库
```

这就是一个真实 Web 平台的基本结构。后续无论是账号登录、活动报名、新闻发布、资料上传，都会在这个结构上继续扩展。

### 下一步建议验证

继续访问：

```text
http://121.40.156.210/api/health
```

如果它也返回 `"type": "PostgreSQL"`，说明 Nginx 的 `/api` 代理也通了。这样前端以后就可以通过同一个域名调用后端接口，不需要用户额外记住 `3001` 端口。

## 2026-06-03：修正前端上线后的 API 地址

### 这次改了什么
我把 `src/api.js` 里的默认接口地址从：

```js
http://127.0.0.1:3001/api
```

改成了：

```js
/api
```

同时修复了接口请求失败时的中文提示乱码，并把 `.env.example` 的说明文字改成正常中文。

### 为什么要这样改
`127.0.0.1` 的意思是“当前这台电脑自己”。在本地开发时，它指你的电脑；但网站上线后，访问者打开 `http://121.40.156.210` 时，浏览器里的 `127.0.0.1` 会变成访问者自己的电脑，而不是阿里云服务器。

所以正式上线的网站更适合使用 `/api`。它的意思是：在当前网站同一个地址下面请求接口。也就是：

```text
http://121.40.156.210/api/health
```

然后由 Nginx 把 `/api` 转发到服务器内部的后端：

```text
127.0.0.1:3001
```

### 你学到的知识点
- 本地开发地址：常见是 `http://127.0.0.1:3001`。
- 公网上线地址：应该尽量使用同域 `/api`，这样以后绑定域名也不用改前端代码。
- `VITE_API_BASE_URL` 仍然保留，如果以后有特殊部署方式，还可以通过环境变量覆盖默认接口地址。

## 2026-06-03：服务器更新代码时遇到 git pull 冲突

### 这次遇到的问题
你在服务器执行 `git pull` 时，终端提示：

```text
error: Your local changes to the following files would be overwritten by merge:
  package-lock.json
Please commit your changes or stash them before you merge.
Aborting
```

这说明服务器上的 `package-lock.json` 被 `npm install` 改动过了，而 GitHub 上也有新版 `package-lock.json`。Git 为了避免直接覆盖本地文件，所以暂停了合并。

### 这次怎么处理
服务器上的 `package-lock.json` 不是我们要保存的正式内容，只是安装依赖时自动生成的变化，所以可以让它恢复成 GitHub 仓库里的版本：

```bash
git restore package-lock.json
git pull
```

然后再重新安装依赖、构建前端、发布到 Nginx 目录。

### 另一个小问题
你在 Linux 终端里直接输入了：

```bash
http://121.40.156.210
http://121.40.156.210/api/health
```

终端会把它们当成“要执行的命令”，所以报错 `No such file or directory`。

正确验证方法有两种：

- 在浏览器地址栏打开网址。
- 在服务器终端用 `curl` 命令访问网址。

例如：

```bash
curl http://121.40.156.210/api/health
```

`curl` 的意思是“用命令行访问一个网址，并把返回内容显示出来”。

## 2026-06-03：完成公网部署闭环验证

### 这次你完成了什么

你在阿里云 Ubuntu 服务器上完成了代码更新、前端构建、静态文件发布，并用浏览器和命令行验证了后端接口。

你执行的关键命令是：

```bash
cd /opt/aoxiang
git pull
npm install
npm run build
rm -rf /var/www/aoxiang/*
cp -r dist/* /var/www/aoxiang/
curl http://121.40.156.210/api/health
```

### 每一步是什么意思

`cd /opt/aoxiang` 是进入服务器上的项目目录。

`git pull` 是从 GitHub 拉取最新代码。如果显示 `Already up to date.`，说明服务器上的代码已经和 GitHub 一致。

`npm install` 是根据 `package.json` 和 `package-lock.json` 安装项目依赖。它显示 `0 vulnerabilities`，说明这次依赖检查没有发现已知安全漏洞。

`npm run build` 是把 React/Vite 前端项目打包成正式上线用的静态文件，生成在 `dist` 目录里。

`rm -rf /var/www/aoxiang/*` 是清空 Nginx 正在对外提供访问的旧网页文件。

`cp -r dist/* /var/www/aoxiang/` 是把刚构建好的新网页复制到 Nginx 的网站目录。

`curl http://121.40.156.210/api/health` 是用命令行访问后端健康检查接口，确认接口能从公网访问。

### 验证结果

接口返回了：

```json
{"ok":true,"service":"aoxiang-api","database":{"type":"PostgreSQL","host":"127.0.0.1:5432"}}
```

这说明当前上线链路已经打通：

```text
公网 IP 121.40.156.210
-> Nginx 返回前端页面
-> 前端通过 /api 请求后端
-> Nginx 把 /api 转发给 Express 后端
-> Express 后端连接 PostgreSQL 数据库
```

### 你现在学到的核心知识

一个真正上线的网站通常不是只有一个页面文件，而是由几部分协同工作：

- 前端：用户看到的页面。
- 后端：处理报名、登录、证书生成、新闻发布等数据逻辑。
- 数据库：保存志愿者、活动、报名、成果、账号等数据。
- Nginx：负责把公网访问分发给前端页面和后端接口。

下一步要做的是让后端长期稳定运行。现在如果后端是用 `npm run server` 手动启动的，关闭 SSH 窗口后它可能会停止。我们会用 PM2 管理后端进程，让它自动守护和开机自启。

## 2026-06-03：PM2 提示 Script already launched

### 这次看到的现象

你执行：

```bash
pm2 start server/index.js --name aoxiang-api
```

终端提示：

```text
[PM2][ERROR] Script already launched, add -f option to force re-execution
```

### 这是什么意思

这不是失败，而是 PM2 已经发现同一个后端脚本 `server/index.js` 正在运行，并且名字也是 `aoxiang-api`。为了避免重复启动两个相同的后端服务，PM2 阻止了第二次启动。

### 正确处理方式

这种情况下不需要重新启动，直接查看进程列表并保存即可：

```bash
pm2 list
pm2 save
curl http://121.40.156.210/api/health
```

如果以后修改了后端代码，需要让 PM2 重新加载新代码，可以执行：

```bash
pm2 restart aoxiang-api
```

`pm2 restart` 的意思是重启指定的后台进程，适合代码更新后让新代码生效。

## 2026-06-03：PM2 后端守护验证成功

### 这次验证结果

你执行了：

```bash
pm2 list
pm2 save
curl http://121.40.156.210/api/health
```

`pm2 list` 显示：

```text
name         status
 aoxiang-api online
```

这说明 `aoxiang-api` 后端服务已经被 PM2 托管，并且当前正在运行。

`curl http://121.40.156.210/api/health` 返回了：

```json
{"ok":true,"service":"aoxiang-api","database":{"type":"PostgreSQL","host":"127.0.0.1:5432"}}
```

这说明公网接口仍然正常，后端和 PostgreSQL 数据库连接也正常。

### 这一步为什么重要

之前用 `npm run server` 启动后端，属于“临时启动”。如果 SSH 窗口关闭、终端中断，后端可能会停止。

现在用 PM2 后，后端变成了“后台守护进程”。它的好处是：

- 可以在后台持续运行。
- 进程异常退出时可以自动拉起。
- `pm2 save` 会保存当前进程列表。
- 配合 `pm2 startup` 后，服务器重启后也能自动恢复服务。

### 后续常用 PM2 命令

查看后端运行状态：

```bash
pm2 list
```

查看后端日志：

```bash
pm2 logs aoxiang-api
```

更新后端代码后重启服务：

```bash
pm2 restart aoxiang-api
```

停止后端服务：

```bash
pm2 stop aoxiang-api
```

现在，网站已经从“能访问”升级到了“可以稳定运行”。
